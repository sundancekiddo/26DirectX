1. virtual void Start() = 0; 순수가상함수
2. 106줄
3.
4. Run()함수 속 while문의 조건절이 잘못됨. isRunning은 initalize 되면 true 상태이므로 while문의 조건으로 !isRunning을 isRunning으로 작성해야함.
5.for (int i = 0; i < components.size(); i++) {
            delete components[i];
        }

답:
1. 순수 가상함수.
2. 106줄.
3. speed와 delta time을 곱한다는걸 알아야 프레임과 독립적으로 동작함.
4. !isRunning이라는 조건절 잘못됨.(104줄)
5. components의 size() 만큼 반복문을 돌면서 delete.