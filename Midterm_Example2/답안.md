1.constant buffer set 하기전 constant buffer를 subresource update 하는 dst로 지정함. 아니면 subresource dst를 0으로 설정해 제대로 지정하지 않음.
2.PS_INPUT VS_Main(VS_INPUT input) 
        {
            PS_INPUT output;
            
            float s = sin(g_Rotation);
            float c = cos(g_Rotation);
            
            float3 rotPos = input.pos;
            rotPos.y = input.pos.x * c + input.pos.y * s;
            rotPos.x = input.pos.x * s + input.pos.y * c;

            float3 finalPos = rotPos;
            finalPos.x -= g_Offset.x;
            finalPos.y -= g_Offset.y;

            output.pos = float4(finalPos, 1.0f);
            output.col = input.col;
            return output;
        }

 
답:
2.         rotPos.y = input.pos.x * c - input.pos.y * s;
            rotPos.x = input.pos.x * s + input.pos.y * c;
            ...
            finalPos.x += g_Offset.x;
            finalPos.y += g_Offset.y;

1. gpu는 128개의 버스를 가지고있음. 항상 4개의 값을 넣어야함. padding 필요.



어려운 문제-> 쉐이더에서 나옴.
C++관련 지식. 순수가상함수 문제 같은 것.
키 눌림 처리, delta time 계산. 회전, 원점 기준 확대&축소, 이동 관련해서 계산하는 것 나옴..(좌표 계산 문제)
버그 찾기. 버그 일어난 자리 두고 원인 찾기.
Lecture 01 ~ 05.
